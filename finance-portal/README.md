# Jixels Finance

Start the site from this folder with `node server.js`, then open `http://localhost:5500`.

The portal is fully offline: it uses only the local CSS and JavaScript files in this project. The server is recommended so every linked file loads consistently.
